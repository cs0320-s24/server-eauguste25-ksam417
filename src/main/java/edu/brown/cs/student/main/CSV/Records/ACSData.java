package edu.brown.cs.student.main.CSV.Records;

/**
 * A record that represents the data, percentage of internet bandwidth returned by the ACS API
 *
 * @param percent
 */
public record ACSData(String percent) {}
